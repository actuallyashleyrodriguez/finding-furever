<div class="showPet">
<h1><%= @pet.name %></h1>
<%= image_tag(@pet.image_url)%>
<h3>Age: <%= @pet.age%> | Breed: <%= @pet.breed%></h3>
</div>


<p>Interested in <%= @pet.name %>? <br>
Fill out an <%= link_to "application", new_application_path %> with <%= @pet.shelter.name%> to set up your initial meet and greet!</p>
<p><%= @pet.name %> has <%= pluralize(@pet.applications.count, "application") %> pending </p>

<p><% if current_admin == @pet.shelter.admin %>
Was <%= @pet.name %> Adopted? <%= button_to "Delete this Pet", pet_path(@pet), method: :delete %>
<% end %>
</p>
